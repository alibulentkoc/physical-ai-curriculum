# Requirements to build and preview the student-facing site.
# Install:  pip install -r requirements-docs.txt
# Preview:  mkdocs serve     (then open http://127.0.0.1:8000)
# Build:    mkdocs build     (outputs to ./site, which is gitignored)

mkdocs-material
pymdown-extensions
