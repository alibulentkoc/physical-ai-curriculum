# Answer Key — Lesson 2.2: The Planar Two-Link Arm: Geometry of the Solution

**Coaches only.** Formative.

1. **Elbow angle source** — the law of cosines on the base–elbow–target triangle: cos θ₂ = (r²−L₁²−L₂²)/(2L₁L₂).
2. **cos θ₂ ∈ [−1,1] iff** — |L₁−L₂| ≤ r ≤ L₁+L₂ (the reachability condition).
3. **Shoulder = atan2(y,x) − β** — True.
4. **L1=0.4, L2=0.3, target (0.5,0): cos θ₂ (short).** 0 — a right-angle elbow (0.25 − 0.16 − 0.09 = 0, over 0.24).

**Challenge rubric.** Full credit: set cos θ₂ = +1 → r² = (L₁+L₂)² → r = L₁+L₂; set cos θ₂ = −1 → r² = (L₁−L₂)² → r = |L₁−L₂|; since cos is monotone on [0,π], cos θ₂ ∈ [−1,1] exactly for |L₁−L₂| ≤ r ≤ L₁+L₂. Partial: checks only one boundary.
